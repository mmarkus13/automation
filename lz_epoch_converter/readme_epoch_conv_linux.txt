[Usage]
"epoch" is the command which calls the epoch.py script.

epoch 					#will print current date in epoch format
epoch 1583762400 		#will convert your <xxxxxxxxxx> epoch value to human date


[Installation]
mkdir -p /home/$USER/scripts
echo "export PATH=$PATH:/home/$USER/scripts" >> /home/$USER/.bashrc
